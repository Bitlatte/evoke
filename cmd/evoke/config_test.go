package main

import (
	"os"
	"testing"

	"github.com/stretchr/testify/assert"
)

func TestLoadConfig_ParsesEvokeYaml(t *testing.T) {
	// Arrange
	configContent := `
title: "My Awesome Site"
description: "A site generated by Evoke"
`
	os.WriteFile("evoke.yaml", []byte(configContent), 0644)

	// Act
	config, err := loadConfig()

	// Assert
	assert.NoError(t, err)
	assert.Equal(t, "My Awesome Site", config["title"])
	assert.Equal(t, "A site generated by Evoke", config["description"])

	// Clean up
	os.Remove("evoke.yaml")
}
